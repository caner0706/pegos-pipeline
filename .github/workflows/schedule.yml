name: Pegos Live Data Fetch & Predict

on:
  schedule:
    - cron: "0 */4 * * *"   # her 4 saatte bir (UTC)
  workflow_dispatch:

jobs:
  fetch_and_upload:
    runs-on: ubuntu-latest
    timeout-minutes: 50

    env:
      AUTH_TOKEN: ${{ secrets.AUTH_TOKEN }}
      CT0: ${{ secrets.CT0 }}
      HF_TOKEN: ${{ secrets.HF_TOKEN }}
      HF_DATASET_REPO: ${{ secrets.HF_DATASET_REPO }}
      TWITTER_USER: ${{ secrets.TWITTER_USER }}
      TWITTER_PASS: ${{ secrets.TWITTER_PASS }}
      KEEP_LAST: ${{ secrets.KEEP_LAST }}

    steps:
      # === REPO VE ORTAM ===
      - name: 📦 Checkout repository
        uses: actions/checkout@v4

      - name: 🐍 Set up Python 3.10
        uses: actions/setup-python@v5
        with:
          python-version: "3.10"

      - name: 🌐 Install Chrome & dependencies
        run: |
          set -e
          sudo apt-get update
          sudo apt-get install -y wget gnupg
          wget -q -O - https://dl.google.com/linux/linux_signing_key.pub | sudo apt-key add -
          echo "deb [arch=amd64] http://dl.google.com/linux/chrome/deb/ stable main" | sudo tee /etc/apt/sources.list.d/google-chrome.list
          sudo apt-get update
          sudo apt-get install -y google-chrome-stable

      - name: 📦 Install Python dependencies
        run: |
          set -e
          python -m pip install --upgrade pip
          pip install -r requirements.txt
          pip install jupyter  # güvenlik için garanti

      # === DEBUG VE TOKEN KONTROLÜ ===
      - name: 🧩 Debug environment & Hugging Face token
        run: |
          set -e
          echo "=== DEBUG ENVIRONMENT ==="
          echo "HF_DATASET_REPO: ${HF_DATASET_REPO}"
          echo "HF_TOKEN length: ${#HF_TOKEN}"
          echo "AUTH_TOKEN length: ${#AUTH_TOKEN}"
          echo "CT0 length: ${#CT0}"
          python - <<'PY'
          import os
          from huggingface_hub import HfApi
          token = os.getenv("HF_TOKEN")
          repo = os.getenv("HF_DATASET_REPO")
          api = HfApi(token=token)
          try:
              print("✅ HF Token Valid:", bool(api.whoami()))
              print("✅ Repo Access:", bool(api.repo_info(repo_id=repo, repo_type='dataset')))
          except Exception as e:
              print("⚠️ HF Access Error:", e)
          PY
          echo "==========================="

      # === SCRAPER ===
      - name: 🕸️ Run scraper notebook (data.ipynb)
        env:
          AUTH_TOKEN: ${{ secrets.AUTH_TOKEN }}
          CT0: ${{ secrets.CT0 }}
        run: |
          set -e
          echo "🚀 Starting Pegos Twitter Scraper..."
          jupyter nbconvert --to notebook --execute data.ipynb --output output.ipynb --ExecutePreprocessor.timeout=1800
          echo "✅ Notebook executed successfully"

      # === UPLOAD ===
      - name: ☁️ Upload scraped data to Hugging Face
        env:
          HF_TOKEN: ${{ secrets.HF_TOKEN }}
          HF_DATASET_REPO: ${{ secrets.HF_DATASET_REPO }}
        run: |
          set -e
          TODAY=$(date -u +%F)
          CSV="/tmp/data/${TODAY}/pegos_output.csv"
          echo "📅 Checking CSV at: ${CSV}"
          if [ -f "${CSV}" ]; then
            echo "📤 Uploading to HF (daily folder)..."
            export LOCAL_CSV="${CSV}"
            python upload_to_hf.py
          else
            echo "⚠️ No CSV found, skipping upload."
          fi

      # === BUILD ===
      - name: 🧱 Merge tweets + BTC data
        env:
          HF_TOKEN: ${{ secrets.HF_TOKEN }}
          HF_DATASET_REPO: ${{ secrets.HF_DATASET_REPO }}
        run: |
          set -e
          echo "🔄 Building merged dataset..."
          python build_dataset_hf.py || echo "⚠️ build_dataset_hf.py failed — continuing"
          echo "✅ Merged dataset uploaded to HF."

      # === CLEAN ===
      - name: 🧹 Clean merged dataset
        env:
          HF_TOKEN: ${{ secrets.HF_TOKEN }}
          HF_DATASET_REPO: ${{ secrets.HF_DATASET_REPO }}
        run: |
          set -e
          echo "🧽 Cleaning dataset..."
          python clean_dataset_hf.py || echo "⚠️ clean_dataset_hf.py failed — continuing"
          echo "✅ Cleaned dataset uploaded."

      # === PREDICT ===
      - name: 🤖 Run Pegos Hybrid Prediction
        env:
          HF_TOKEN: ${{ secrets.HF_TOKEN }}
          HF_DATASET_REPO: ${{ secrets.HF_DATASET_REPO }}
        run: |
          set -e
          echo "🧠 Running Pegos hybrid prediction pipeline..."
          python predict_with_model.py || echo "⚠️ predict_with_model.py failed — continuing"
          echo "✅ Hybrid predictions completed and uploaded."

      # === VALIDASYON ===
      - name: 📜 List HF dataset files
        if: always()
        env:
          HF_TOKEN: ${{ secrets.HF_TOKEN }}
          HF_DATASET_REPO: ${{ secrets.HF_DATASET_REPO }}
        run: |
          python - <<'PY'
          import os
          from huggingface_hub import HfApi
          api = HfApi(token=os.getenv("HF_TOKEN"))
          repo = os.getenv("HF_DATASET_REPO")
          print("📂 Listing files in", repo)
          for f in api.list_repo_files(repo_id=repo, repo_type="dataset"):
              print(" -", f)
          PY

      # === DEBUG ÇIKTILARI ===
      - name: 🗂️ Show workspace & temp data
        if: always()
        run: |
          echo "📁 Workspace contents:"
          ls -lah
          echo
          echo "📅 Daily data folders:"
          ls -lah /tmp/data || true

      - name: 🪶 Show notebook summary
        if: always()
        run: |
          echo "📜 Notebook Output (short):"
          head -n 200 output.ipynb | sed 's/\\n/\n/g' | grep -E "✅|⚠️|❌|tweet|Upload|Total" || true
